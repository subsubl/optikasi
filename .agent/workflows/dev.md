---
description: Start the Nuxt development server
---

# Start Development Server

This workflow starts the Nuxt development server for local development.

## Steps

// turbo
1. Start the development server:
```bash
npm run dev
```

The server will be available at http://localhost:3000

## Notes

- The dev server supports hot module replacement (HMR)
- Press `Ctrl+C` to stop the server
- Dev tools are enabled in development mode (see `nuxt.config.ts`)
